# Docker Environment Configuration for ask_llm
# Copy this file to .env and fill in your values

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_USER=askllm
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=askllm
POSTGRES_PORT=5432

# =============================================================================
# Service Ports (External)
# =============================================================================
MCP_PORT=8001
SERVICE_PORT=8642

# =============================================================================
# LLM Provider API Keys
# =============================================================================

# OpenAI (GPT-4, GPT-3.5, etc.)
OPENAI_API_KEY=

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# Ollama (if using external Ollama instance)
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# =============================================================================
# Search Providers (Optional)
# =============================================================================

# Tavily Search (Production search, requires API key)
TAVILY_API_KEY=

# DuckDuckGo is used by default (free, no key needed)

# =============================================================================
# Application Settings
# =============================================================================

# Default bot to use
# ASK_LLM_DEFAULT_BOT=nova

# Default model
# ASK_LLM_DEFAULT_MODEL=gpt-4

# Conversation history limit
# ASK_LLM_HISTORY_LIMIT=10

# Enable verbose logging
# ASK_LLM_VERBOSE=false

# Enable debug logging
# ASK_LLM_DEBUG=false

# =============================================================================
# Local Models (Optional)
# =============================================================================

# Path to local GGUF models (mounted as volume)
MODELS_PATH=./models

# =============================================================================
# Advanced Configuration
# =============================================================================

# Database connection string (auto-constructed from above if not set)
# ASK_LLM_DATABASE_URL=postgresql://askllm:password@postgres:5432/askllm

# Memory server URL (internal)
# ASK_LLM_MEMORY_SERVER_URL=http://127.0.0.1:8001

# Service host/port (inside container)
# ASK_LLM_MEMORY_SERVER_HOST=0.0.0.0
# ASK_LLM_MEMORY_SERVER_PORT=8001
# ASK_LLM_SERVICE_HOST=0.0.0.0
# ASK_LLM_SERVICE_PORT=8642

# Log prefix
# ASK_LLM_LOG_PREFIX=docker

# =============================================================================
# Notes
# =============================================================================
#
# - Required fields: POSTGRES_PASSWORD and at least one API key (OPENAI_API_KEY or ANTHROPIC_API_KEY)
# - For local models, place .gguf files in ./models/ directory
# - For custom bots/models, edit config/bots.yaml and config/models.yaml
# - See README-DOCKER.md for detailed configuration guide
#
